---
title: API Reference
description: Complete URL specification, query parameters, and response formats.
---

## URL format

<InlineHint variant="tip">All endpoints support both `.png` and `.svg` — swap `.json` to get raw badge data, or append `/shields.json` for shields.io compatibility.</InlineHint>

<CodeLine language="bash" code="https://shieldcn.dev/{provider}/{...params}.png" />
<CodeLine language="bash" code="https://shieldcn.dev/{provider}/{...params}.svg" />
<CodeLine language="bash" code="https://shieldcn.dev/{provider}/{...params}.json" />
<CodeLine language="bash" code="https://shieldcn.dev/{provider}/{...params}/shields.json" />

## Badge endpoints

### npm

| Endpoint | Description |
|----------|-------------|
| `/npm/{package}.svg` | Latest version |
| `/npm/{package}/downloads.svg` | Weekly download count |
| `/npm/@scope/package.svg` | Scoped package version |

### GitHub

| Endpoint | Description |
|----------|-------------|
| `/github/{owner}/{repo}/stars.svg` | Star count |
| `/github/{owner}/{repo}/release.svg` | Latest release tag |
| `/github/{owner}/{repo}/ci.svg` | CI workflow status |
| `/github/{owner}/{repo}/license.svg` | SPDX license identifier |
| `/github/downloads/{owner}/{repo}.svg` | Downloads — all assets, all releases |
| `/github/downloads/{owner}/{repo}/latest.svg` | Downloads — all assets, latest release |
| `/github/downloads/{owner}/{repo}/{tag}.svg` | Downloads — all assets, specific tag |
| `/github/downloads-asset/{owner}/{repo}/{asset}.svg` | Downloads — specific asset, all releases |
| `/github/downloads-asset/{owner}/{repo}/{asset}/latest.svg` | Downloads — specific asset, latest release |
| `/github/downloads-asset/{owner}/{repo}/{asset}/{tag}.svg` | Downloads — specific asset, specific tag |

### Discord

| Endpoint | Description |
|----------|-------------|
| `/discord/{serverId}.svg` | Online member count |
| `/discord/members/{inviteCode}.svg` | Approximate member count from invite |
| `/discord/online-members/{inviteCode}.svg` | Approximate online member count from invite |

### X / Twitter

| Endpoint | Description |
|----------|-------------|
| `/x/{username}.svg` | Follow CTA (default) |
| `/x/follow/{username}.svg` | "follow @username" CTA |
| `/x/mention/{username}.svg` | "@username" mention CTA |

Static badges — no API token required.

### Homebrew

| Endpoint | Description |
|----------|-------------|
| `/homebrew/{formula}.svg` | Formula version |
| `/homebrew/cask/{cask}.svg` | Cask version |
| `/homebrew/dm/{formula}.svg` | Formula downloads (monthly) |
| `/homebrew/dq/{formula}.svg` | Formula downloads (quarterly) |
| `/homebrew/dy/{formula}.svg` | Formula downloads (yearly) |
| `/homebrew/cask-dm/{cask}.svg` | Cask downloads (monthly) |
| `/homebrew/cask-dq/{cask}.svg` | Cask downloads (quarterly) |
| `/homebrew/cask-dy/{cask}.svg` | Cask downloads (yearly) |

### Tokscale

| Endpoint | Description |
|----------|-------------|
| `/tokscale/{username}.svg` | Total token count (default) |
| `/tokscale/tokens/{username}.svg` | Total token count |
| `/tokscale/cost/{username}.svg` | Estimated total cost |
| `/tokscale/rank/{username}.svg` | Global leaderboard rank |
| `/tokscale/active-days/{username}.svg` | Days with AI activity |
| `/tokscale/stats.svg` | Global user count |

### Country Flags

| Endpoint | Description |
|----------|-------------|
| `/flag/{code}.svg` | “built in _country_” with a flag chip |

`{code}` is an ISO 3166-1 alpha-2 country code (case-insensitive), plus regions
and subdivisions like `eu`, `gb-eng`, `es-ct`. 265 codes supported. Definite
articles are applied automatically (`us` → “the USA”). See [Country Flags docs](/docs/badges/flag).

### Badge Group

| Endpoint | Description |
|----------|-------------|
| `/group/{badge1}+{badge2}+{badge3}.svg` | Multiple badges joined in one image |

Join any badge paths with `+`. Query params apply to all segments. See [Badge Group docs](/docs/badges/group).

### Static

| Endpoint | Description |
|----------|-------------|
| `/badge/{label}-{message}-{color}.svg` | Custom text badge |
| `/badge/dynamic/json.svg?url=...&query=...` | Dynamic JSON badge |

## Query parameters

<ApiRefTable
  title="Appearance"
  props={[
    {
      name: "variant",
      type: '"default" | "secondary" | "outline" | "ghost" | "destructive" | "branded"',
      default: '"default"',
      description: "shadcn Button variant. Controls background, text color, and border.",
    },
    {
      name: "size",
      type: '"xs" | "sm" | "default" | "lg"',
      default: '"sm"',
      description: "shadcn Button size. xs=24px, sm=32px, default=36px, lg=40px.",
    },
    {
      name: "mode",
      type: '"dark" | "light"',
      default: '"dark"',
      description: "Color mode. Dark matches GitHub dark theme. Combine with a <picture> element to auto-adapt to the reader's theme — see Light & Dark Mode.",
    },
    {
      name: "theme",
      type: "string",
      default: "—",
      description: "Color theme override: zinc, slate, blue, green, rose, orange, violet, purple, cyan, emerald.",
    },
    {
      name: "font",
      type: '"inter" | "geist" | "geist-mono" | "jetbrains-mono" | "fira-code" | "roboto" | "space-grotesk"',
      default: '"inter"',
      description: "Font family for badge text. Inter is the default (matches shadcn). Monospace fonts (geist-mono, jetbrains-mono, fira-code) work well for version and code badges.",
    },
  ]}
/>

<ApiRefTable
  title="Colors"
  props={[
    {
      name: "color",
      type: "string",
      default: "—",
      description: "Override badge background color (hex without #).",
    },
    {
      name: "labelColor",
      type: "string",
      default: "—",
      description: "Override label-side background color for split mode (hex without #).",
    },
    {
      name: "valueColor",
      type: "string",
      default: "—",
      description: "Override value text color (hex without #).",
    },
    {
      name: "labelTextColor",
      type: "string",
      default: "—",
      description: "Override label text color (hex without #).",
    },
    {
      name: "labelOpacity",
      type: "number",
      default: "0.7",
      description: "Label text opacity (0-1). Set to 1 for full visibility.",
    },
    {
      name: "gradient",
      type: "string",
      default: "\u2014",
      description: "Comma-separated hex colors for a linear gradient background. Optionally append an angle (0\u2013360) as the last value. Example: ff6b6b,4ecdc4 or ff6b6b,4ecdc4,135.",
    },
  ]}
/>

<ApiRefTable
  title="Icons"
  props={[
    {
      name: "logo",
      type: "string | false",
      default: "auto",
      description: "SimpleIcons slug (e.g. react, typescript), React Icons (e.g. ri:FaReact, ri:GoStarFill), a base64 SVG data URI (data:image/svg+xml;base64,...) for custom icons, or false to hide.",
    },
    {
      name: "logoColor",
      type: "string",
      default: "—",
      description: "Override logo color (hex without #).",
    },
  ]}
/>

<ApiRefTable
  title="Layout"
  props={[
    {
      name: "label",
      type: "string",
      default: "auto",
      description: "Override left-side label text.",
    },
    {
      name: "split",
      type: "boolean",
      default: "false",
      description: "Two-tone background: label side + value side with different colors.",
    },
    {
      name: "statusDot",
      type: "boolean",
      default: "auto",
      description: "Show a colored dot before the label. Auto-enabled for CI badges.",
    },
    {
      name: "height",
      type: "number",
      description: "Override badge height in px.",
    },
    {
      name: "fontSize",
      type: "number",
      description: "Override font size in px.",
    },
    {
      name: "radius",
      type: "number",
      description: "Override border radius in px.",
    },
    {
      name: "padX",
      type: "number",
      description: "Override horizontal padding in px.",
    },
    {
      name: "iconSize",
      type: "number",
      description: "Override icon size in px.",
    },
    {
      name: "gap",
      type: "number",
      description: "Override gap between icon and text in px.",
    },
    {
      name: "labelGap",
      type: "number",
      description: "Override gap between label and value text in px.",
    },
  ]}
/>

<ApiRefTable
  title="Effects"
  props={[
    {
      name: "animate",
      type: "pulse | glow | shimmer",
      default: "none",
      description: "Animate the badge with pure CSS. pulse and glow animate the status dot; shimmer sweeps a highlight across the badge. SVG only — PNG/JSON ignore it. Respects prefers-reduced-motion (static for motion-sensitive visitors).",
    },
  ]}
/>

<InlineHint variant="note">Animation is SVG-only and works inside GitHub's image sandbox because it uses pure CSS keyframes — no JavaScript. `pulse` and `glow` require a status dot (auto for CI badges, or set `statusDot=true` with a `color`).</InlineHint>

## Response formats

### PNG

<CodeBlock language="http" code={"Content-Type: image/png\nCache-Control: public, max-age=3600, s-maxage=3600, stale-while-revalidate=86400"} />

PNG badges are rasterized at the exact badge dimensions. Recommended for GitHub READMEs and maximum cross-platform compatibility.

### SVG

<CodeBlock language="http" code={"Content-Type: image/svg+xml\nCache-Control: public, max-age=3600, s-maxage=3600, stale-while-revalidate=86400"} />

SVG badges are scalable and have smaller file sizes. Use `.svg` when you need crisp rendering at any scale.

### JSON

<CodeBlock language="json" code={'{\n  "label": "npm",\n  "value": "v19.1.0",\n  "color": "blue",\n  "link": "https://www.npmjs.com/package/react"\n}'} />

### Shields.io compatible

<CodeBlock language="json" code={'{\n  "schemaVersion": 1,\n  "label": "npm",\n  "message": "v19.1.0",\n  "color": "blue"\n}'} />

Use with shields.io endpoint badge:

<CodeLine language="markdown" code="![npm](https://img.shields.io/endpoint?url=https://shieldcn.dev/npm/react/shields.json)" />
